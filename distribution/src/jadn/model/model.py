class InformationModel:
    def __init__(self):
        self.encoding = []  # Encoding instances (serialization/encoding rules)
        self.schema = []    # Schema instances (abstract schema packages)
        self.format = []    # Format instances (semantic validation methods)
